#include "main.h"

/**
 * _abs - gives the absolute value of an integer
 * @n: num to check
 * Return: the absolute value of num
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
